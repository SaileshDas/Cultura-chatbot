# Example environment variables for Cultura Chatbot
# Copy this file to `.env` (for local development) or set the values in your deployment
# Do NOT commit a `.env` file that contains real secrets or API keys.

# Comma-separated Gemini API keys (primary, backup1, backup2...)
# Example: your-primary-gemini-key,your-backup-key-1,your-backup-key-2
GEMINI_API_KEYS=your-primary-gemini-key,your-backup-key-1,your-backup-key-2

# JWT secret used to sign session tokens (change in production)
JWT_SECRET=your-jwt-secret

# Gemini retry/backoff settings (tune if you see temporary 5xx/503 errors)
# `GEMINI_ATTEMPTS_PER_KEY` = total attempts per API key (default: 3)
# `GEMINI_RETRY_BASE_MS` = base backoff in milliseconds (default: 500)
GEMINI_ATTEMPTS_PER_KEY=3
GEMINI_RETRY_BASE_MS=500

# Global retry cycles across all keys (default: 2)
# If you still see frequent 5xx/503 errors, increase this value conservatively.
GEMINI_GLOBAL_RETRIES=2
GEMINI_GLOBAL_BACKOFF_MS=1000

# Optional graceful fallback when all Gemini attempts fail
# Set to 'true' to return a friendly fallback message instead of a 500 error
GEMINI_ALLOW_FALLBACK=false
# Text returned when fallback is enabled
GEMINI_FALLBACK_TEXT=The AI model is temporarily unavailable. Please try again later.

# Frontend build-time/runtime values
VITE_API_BASE=http://localhost:3001

# If you host a separate Python TTS/STT server, set its base URL here (optional)
VITE_TTS_BASE=http://localhost:5000

# Optional: default voice for Edge TTS (used by Python server)
EDGE_TTS_VOICE=en-IN-PrabhatNeural
